# Salas de espacios.
# Cada sala es una agrupación de personas comunicándose alrededor de un tema específico
# de un espacio.
# Por ejemplo: nombre de materia en Aula Virtual.
#
# Campos:
# Id - Identificador único
# SpaceId - Referencia al espacio
# Name - Nombre de sala
# Tag - Etiqueta de agrupación de salas, usada para relacionar varias salas en un espacio
# CreatedAt - Fecha y hora de creación de sala
# Disabled - Si está habilitada

DROP TABLE IF EXISTS `TP_RealtimeSpaceRooms`;

CREATE TABLE `TP_RealtimeSpaceRooms` (
  `Id` VARCHAR(64) NOT NULL,
  `SpaceId` VARCHAR(64) NOT NULL,
  `Name` VARCHAR(255) NOT NULL,
  `Tag` VARCHAR(255) DEFAULT NULL,
  `CreatedAt` DATETIME,
  `Disabled` TINYINT(1) DEFAULT 0,
  PRIMARY KEY (`Id`),
  FOREIGN KEY (SpaceId) REFERENCES TB_RealtimeSpaces(Id) ON DELETE CASCADE
) ENGINE=MyISAM DEFAULT CHARSET=latin1;
