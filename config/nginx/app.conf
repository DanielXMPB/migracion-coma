resolver 127.0.0.11 valid=100s;

# ----------------------------------------------- #
#               FISICOMECANICAS                   #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_fifme";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name fisicomecanicas.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# uisinnova
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name uisinnova.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://fisicomecanicas.uis.edu.co/eisi/grupo/uisinnova$request_uri;
}

# labolsa (PENDIENTE)
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name labolsa.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://fisicomecanicas.uis.edu.co/eisi/grupo/uisinnova$request_uri;
}

# linkingup
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name linkingup.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://fisicomecanicas.uis.edu.co/eisi/grupo/linkingup$request_uri;
}

# ----------------------------------------------- #
#                   SISTEMAS                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_eisi";
    set $port           8080;

    listen 80;
    listen [::]:80;
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name eisidev01.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }

    # Realtime proxy
    include conf.d/include/realtime.conf;
}

# CALUMET
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name calumet.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://ingsistemas.uis.edu.co/eisi/grupo/calumet$request_uri;
}

# ----------------------------------------------- #
#                     CIVIL                       #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_eci";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name ingenieriacivil.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# INME
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name inme.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://ingenieriacivil.uis.edu.co/eisi/grupo/calumet$request_uri;
}

# GPH
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name gph.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://ingenieriacivil.uis.edu.co/eisi/grupo/gph$request_uri;
}

# simposiopuentes2018
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name simposiopuentes2018.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://ingenieriacivil.uis.edu.co/eisi/grupo/simposiopuentes2018$request_uri;
}

# ----------------------------------------------- #
#             DISEÑO INDUSTRIAL                   #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_edi";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name disindustrial.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                   INDUSTRIAL                    #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_eeie";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name industrial.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# OPALO
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name opalo.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://industrial.uis.edu.co/eisi/grupo/opalo$request_uri;
}

# fym
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name fym.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://industrial.uis.edu.co/eisi/grupo/finandman$request_uri;
}

# posgradoseeie
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name posgradoseeie.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://industrial.uis.edu.co/eisi/grupo/posgradoseeie$request_uri;
}

# innotec
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name innotec.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://industrial.uis.edu.co/eisi/grupo/innotec$request_uri;
}

# motivatic
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name motivatic.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://industrial.uis.edu.co/eisi/grupo/motivatic$request_uri;
}

# galea
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name galea.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://industrial.uis.edu.co/eisi/grupo/galea$request_uri;
}


# ----------------------------------------------- #
#                      E3T                        #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_e3t";
    set $port           8080;

    listen 80;
    listen [::]:80;
    listen 443 ssl;
    listen [::]:443 ssl;

    server_name eisidev02.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# cidlis
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name cidlis.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://e3t.uis.edu.co/eisi/grupo/cidlis$request_uri;
}

# cemos
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name cemos.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://e3t.uis.edu.co/eisi/grupo/cemos$request_uri;
}

# posgradose3t
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name posgradose3t.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://e3t.uis.edu.co/eisi/grupo/posgradose3t$request_uri;
}

# ----------------------------------------------- #
#                    MECANICA                     #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_eim";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name mecanicaxserver.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# posgradosmecanica
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name posgradosmecanica.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://mecanicaxserver.uis.edu.co/eisi/grupo/posgradosmecanica$request_uri;
}

# dicbot
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name dicbot.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicomecanicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://mecanicaxserver.uis.edu.co/eisi/grupo/dicbot$request_uri;
}


# ----------------------------------------------- #
#                 FISICOQUIMICAS                  #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_fifqui";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name fisicoquimicas.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicoquimicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                     GEOLOGIA                    #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_esgeo";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name geologia.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicoquimicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                    PETROLEOS                    #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_eip";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name petroleos.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicoquimicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# simposiogeomecanica (PENDIENTE)
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name simposiogeomecanica.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicoquimicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://petroleos.uis.edu.co/eisi/grupo/dicbot$request_uri;
}

# git
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name git.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicoquimicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://petroleos.uis.edu.co/eisi/grupo/git$request_uri;
}

# posgradospetroleos
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name posgradospetroleos.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicoquimicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://petroleos.uis.edu.co/eisi/grupo/posgradospetroleos$request_uri;
}

# ----------------------------------------------- #
#                  METALURGICA                    #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_eimt";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name metalurgia.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicoquimicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#             INGENIERIA QUIMICA                  #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_eiq";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name iq.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/fisicoquimicas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                   CIENCIAS                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_cie";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name ciencia.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/ciencias-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                   BIOLOGIA                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_bio";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name bio.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/ciencias-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                     FISICA                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_esfis";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name fis.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/ciencias-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                 MATEMATICAS                     #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_mat";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name mat.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/ciencias-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                    QUIMICA                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_esquim";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name quim.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/ciencias-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                    HUMANAS                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_fch";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name humanas.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                     ARTES                       #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_eart";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name escartes.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                   DERECHO                       #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_eder";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name derecho.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                   ECONOMIA                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_e2con";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name esceconomia.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                   EDUCACION                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_edu";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name educacion.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# paidopolis
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name paidopolis.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://educacion.uis.edu.co/eisi/grupo/paidopolis$request_uri;
}

# paidofest
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name paidofest.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://educacion.uis.edu.co/eisi/grupo/paidofest$request_uri;
}

# ----------------------------------------------- #
#                   FILOSOFIA                     #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_esfil";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name escuelafilosofia.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                   HISTORIA                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_his";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name historia.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                    IDIOMAS                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_idio";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name eidiomas.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                TRABAJO SOCIAL                   #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_trs";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name trabajosocial.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/humanas-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                     SALUD                       #
# ----------------------------------------------- #


# ----------------------------------------------- #
#                  ENFERMERIA                     #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_esenf";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name enfermeria.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/salud-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                 FISIOTERAPIA                    #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_efis";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name fisioterapia.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/salud-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                    MEDICINA                     #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_esmed";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name medicina.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/salud-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                MICROBIOLOGIA                    #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_em";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name microbiologia.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/salud-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# ----------------------------------------------- #
#                   NUTRICION                     #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_nutri";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name nutricion.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/salud-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}


# ----------------------------------------------- #
#                     OTROS                       #
# ----------------------------------------------- #

# ----------------------------------------------- #
#                    VACADEM                      #
# ----------------------------------------------- #

server {
    set $forward_scheme http;
    set $server         "coma_vac";
    set $port           8080;

    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name vacadem.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/otros-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    location = /{
        return 301 $scheme://$http_host/eisi/;
    }

    location / {
        # Proxy!
        include conf.d/include/proxy.conf;
    }
}

# summerschool
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name summerschool.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/otros-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://vacadem.uis.edu.co/eisi/grupo/summerschooluis$request_uri;
}

# cultural (PENDIENTE)
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name summerschool.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/otros-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://vacadem.uis.edu.co/eisi/grupo/summerschooluis$request_uri;
}

# cultural intercambiovirtual
server {
    listen 80;
    listen [::]:80;
    #listen 443 ssl;
    #listen [::]:443 ssl;

    server_name intercambiovirtual.uis.edu.co;
    server_tokens off;

    # Let's Encrypt acme challenge
    include conf.d/include/letsencrypt-acme-challenge.conf;
    # Ssl key
    #include conf.d/include/certificates/otros-cert.conf;
    # Force ssl
    #include conf.d/include/force-ssl.conf;
    # Larger timeout
    include conf.d/include/timeout.conf;

    return 301 https://vacadem.uis.edu.co/eisi/grupo/pasantiainvestigacion$request_uri;
}