services:
  actions-runner:
    image: untetheredj/actions-runner:latest
    container_name: actions-runner
    restart: always
    environment:
      - REPO="calumet/coma"
      - TOKEN="1234"
    deploy:
      resources: 
        limits:
          cpus: '0.20'
          memory: 1G
        reservations:
          memory: 512MB
networks:
  coma_network:
    driver: bridge
    name: coma_network
